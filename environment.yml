name: clinic_llm_eval_env
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.11
  - pip
  - scikit-learn
  - numpy
  - pip:
      - deepeval
      - langchain-openai
      - langchain-google-genai
      - jinja2
      - python-dotenv
      - pytest