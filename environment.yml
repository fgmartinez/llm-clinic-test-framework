name: clinic_llm_eval_env
channels:
  - defaults
  - conda-forge
dependencies:
  - python=3.11
  - pip
  - pip:
      - deepeval
      - langchain-openai
      - langchain-google-genai
      - jinja2
      - scikit-learn
      - numpy
      - python-dotenv
      - pytest